# Provides PLATFORMS_INClUDE, PLATFORMS_SOURCE

set(LWM2M_INCLUDE ${CMAKE_CURRENT_LIST_DIR})
aux_source_directory(${CMAKE_CURRENT_LIST_DIR} LWM2M_SOURCE)
if(WITH_LOGS)
    set(LWM2M_DEFINITIONS -DLWM2M_WIGH_LOGS -DCOAP_WITH_LOGS)
endif()