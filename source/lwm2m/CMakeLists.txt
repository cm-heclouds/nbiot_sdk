# Provides LWM2M_INCLUDE, LWM2M_SOURCE, LWM2M_DEFINITIONS

set(LWM2M_INCLUDE ${CMAKE_CURRENT_LIST_DIR})
aux_source_directory(${CMAKE_CURRENT_LIST_DIR} LWM2M_SOURCE)

if(WITH_LOGS)
    set(LWM2M_DEFINITIONS -DLWM2M_WITH_LOGS -DCOAP_WITH_LOGS)
endif()

if(BOOTSTRAP)
    set(LWM2M_DEFINITIONS ${LWM2M_DEFINITIONS} -DLWM2M_BOOTSTRAP)
endif()