# Provides PLATFORMS_INClUDE, PLATFORMS_SOURCE

set(DTLS_INCLUDE ${CMAKE_CURRENT_LIST_DIR})
aux_source_directory(${CMAKE_CURRENT_LIST_DIR} DTLS_SOURCE)
if(WITH_LOGS)
    set(DTLS_DEFINITIONS -DDTLS_WITH_LOGS -DHAVE_DTLS -DDTLS_ECC -DDTLSv12 -DWITH_SHA256 -DDTLS_CHECK_CONTENTTYPE)
endif()